#![cfg(feature = "bitmap")]

// The legacy CPU-readable capture output API was removed in Phase 19X.
// The `bitmap` Cargo feature is left in place for dependency compatibility,
// but it no longer exposes public capture output.
